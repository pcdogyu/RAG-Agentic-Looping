# RAG-Agentic-Looping 第二期开发需求

版本：2.0

日期：2026-09-09

目标分支：`golang`
基线提交：`ac052c62af93e2943e6f2eb19669adb0e55f9825`

## 1. 背景与目标

第一期已经完成评级语义拆分、证据门禁、时点约束、基本面预测/估值/评级的确定性计算、短期预测和校准的安全降级、模型治理及跨市场隔离。第二期不重复这些模块，目标是补齐原任务清单中尚未通过真实数据验收的十项能力：`P1-09`、`P2-01` 至 `P2-07`、`P3-01`、`P3-02`。

第二期必须形成以下闭环：

1. 无新闻时也能基于当时可用的财务、价格、基准和已批准假设触发基本面复核。
2. 行情、公司行动、基准和历史证券池形成不可变、可追溯的真值层。
3. 训练、校准和未来测试严格按时间与事件簇隔离。
4. 只有真实独立样本通过门禁后才发布概率或切换模型。
5. 反方研究和新增资产模型必须用消融或分市场报告证明价值，不能用 Agent 数量或开发完成度代替效果证据。

## 2. 强制约束

- 第一方生产运行时保持 Go，未经单独架构批准不得引入 Python 服务、依赖清单或镜像。
- 缺失价格、基准、公司行动、财务、假设、模型或校准器时返回明确不可用状态，禁止写入零值或估算值冒充事实。
- 所有行情和财务记录必须保存 `observed_at`、`available_at`、来源、币种、字段口径和原始身份。
- 训练样本只能使用训练截止时已经成熟且可获得的标签；同一事件及转载簇不得跨越训练、校准和测试集合。
- 模型、校准器和评级切换必须经过 Shadow、正确性硬门禁和人工批准；漂移只能触发告警与复核。
- 每个步骤必须通过相关单元测试、PostgreSQL 集成测试、Go CI；涉及前端时还需通过 Vitest 和生产构建。
- 每个可交付步骤独立提交、推送到 `origin/golang`，并按仓库发布规范部署和读回验证。

## 3. 开发里程碑

### M1：真实数据基础与自动基本面复核

#### M1.1 不可变行情观测

- 建立价格观测表，区分 `close` 与 `adjusted_close`。
- 保存交易日、实际观测时间、系统可获得时间、币种、来源和日线/时间戳精度。
- 现有结果计算从真实供应商取得价格后同步写入该表；同一来源记录必须幂等。
- 禁止将普通收盘价静默标记为复权价。

验收：重复抓取不产生重复记录；未来可获得的价格不会进入历史截止时间；基准价格使用同一契约。

#### M1.2 公司行动、基准和历史证券池

- 增加拆股、分红、合股、代码变化、停牌和退市事件契约。
- 保存历史证券有效区间和基准映射，不能用当前成分覆盖过去。
- 数据质量报告披露缺失、降级、排除和来源冲突。

验收：拆股与分红样例不会产生虚假收益；缺失基准时相对收益保持不可用。

#### M1.3 无新闻定时基本面研究

- 只选择已跟踪、策略支持且输入完整的标的。
- 财务刷新后比较最新可用快照与上一研究版本；无变化时不重复计算。
- 新估值必须使用可追溯的当前价格、基准和显式批准的假设/情景。
- 输入不完整时记录结构化阻断原因，不创建虚假评级修订。

验收：手动和定时入口对同一输入产生相同版本；任务幂等；失败可重试且不会覆盖历史评级。

### M2：真实结果标签与数据质量

- 实现 1、5、20 个交易日独立成熟标签。
- 从 `signal_available_at` 后第一个可交易时点取价。
- 分开保存绝对收益、基准收益、超额收益和配置成本后的模拟净收益。
- 建立行情覆盖、公司行动覆盖、基准覆盖、退市样本和失败样本报告。

验收：盘后、周末、停牌、拆股、分红、退市和跨市场样例全部通过；未到期样本不提前评价。

### M3：滚动前推数据集

- 生成不可变数据集清单，保存样本、版本、排除原因和特征截止时间。
- 多轮划分训练、独立校准和未来测试窗口。
- 按事件/转载簇隔离，并根据标签期限清除跨边界重叠。
- 最终留出集不能被自动演进或日常调参读取。

验收：任一训练标签在训练截止时已成熟；数据集可完全重现；泄漏测试能主动拦截污染样本。

### M4：可解释基线与独立校准

- 比较恒定基线、简单规则、现有方向分和可解释线性/逻辑模型。
- 分别消融价格、基本面、新闻和预期修订特征。
- 校准样本与训练样本隔离，按资产类别、市场和期限限定适用范围。
- 样本不足、范围不符或校准过期时继续返回 `uncalibrated`。

验收：未来样本报告优于或明确不优于简单基线；制品、预处理和校准版本可复现。

### M5：分层报告、Shadow 与故障演练

- 分开报告研究质量、预测能力、覆盖率和可执行模拟结果。
- 披露拒答、看空、未到期、缺数据、技术失败和退市样本。
- 正式/候选模型以相同截止和执行假设并行运行。
- 演练数据源失效、模型超时、分布漂移、校准失效和人工回滚。

验收：所有告警、批准和回滚均有持久审计；开发集提升不能自动触发上线。

### M6：可选增强验收

- 反方研究先以关闭状态积累人工标签，报告错误发现率、误挑战、延迟和成本；无稳定增量价值则保持关闭。
- 新资产按 ETF、加密资产、商品顺序逐类实现，每类使用独立数据、政策、基准、日历和效果报告。
- 不支持的基本面方法继续返回 `not_applicable`。

## 4. 执行顺序与状态

| 步骤 | 内容 | 状态 |
| --- | --- | --- |
| 2.1 | 第二期需求冻结与不可变行情观测 | 已完成 |
| 2.2 | 公司行动、基准与历史证券池 | 已完成 |
| 2.3 | 无新闻定时基本面研究 | 已完成 |
| 2.4 | 多期限真实结果标签与质量报告 | 已完成 |
| 2.5 | 滚动前推数据集与泄漏门禁 | 已完成（真实生产样本待积累） |
| 2.6 | 可解释基线、消融和独立校准 | 已完成（真实生产实验待样本积累） |
| 2.7 | 分层效果报告、Shadow 和故障演练 | 已完成（真实前瞻样本与持续演练待积累） |
| 2.8 | 反方研究与新增资产模型验收 | 已完成工程门禁（功能默认关闭，真实分市场样本待积累） |
| 2.9 | 真实生产数据冷启动 | 已完成事实数据接线（分析师假设、模型训练与真实成熟样本待积累） |
| 2.10 | 前瞻规则基线样本采集 | 已完成工程接线（仅采集新研究，真实结果待自然成熟） |
| 2.11 | 无新闻基本面人工准备包 | 已完成（真实分析师假设与批准仍不可自动生成） |
| 2.12 | 一致预期首次观测与前瞻修订历史 | 已完成工程接线（真实修订序列从首次观测后积累） |
| 2.13 | 公告语义与管理层指引修订 | 已完成工程接线（生产真实指引修订待可靠来源积累） |
| 2.14 | SEC 原始披露候选与管理层指引人工确认 | 已完成工程接线（生产 SEC 身份与真实人工核验待配置） |
| 2.15 | 无新闻研究真实复权价格冷启动 | 已完成（生产 NVDA 真实复权价格、盘中保护与不可变证据读回已验收） |
| 2.16 | 人工研究到定时计划的同源交接 | 已完成工程接线（真实分析师批准待输入） |
| 2.17 | 分析师证据登记与引用硬门禁 | 已完成工程与生产拒绝路径验收（真实分析师证据待人工登记） |
| 2.18 | 预测、估值与评级直写入口统一证据门禁 | 已完成（生产三类直写拒绝与零副作用已验收） |
| 2.19 | 前瞻结果标签冷启动触发与幂等调度 | 已完成（生产真实任务与未成熟门禁已验收） |
| 2.20 | A 股复权行情端点与来源语义修复 | 已完成（生产回填与残余失败分层已验收） |
| 2.21 | 美股基准资产身份统一 | 已完成（生产策略回读与 Worker 平滑替换已验收） |
| 2.22 | 规范基准资产复权行情冷启动 | 已完成美股样板（A/H 股规范总回报身份由 2.24 补齐） |
| 2.23 | PIT 基准映射人工审批工作台 | 已完成工程与生产未批准路径验收（真实来源、理由和批准人待人工输入） |
| 2.24 | 中港规范总回报基准身份与中证官方行情 | 已完成中证全收益生产闭环及恒指缺源门禁（恒指总回报历史数据源待接入） |
| 2.25 | 持牌恒指总回报行情导入门禁 | 已完成工程与生产拒绝路径验收（真实授权文件、许可证引用和人工审批待提供） |
| 2.26 | 持牌总回报行情人工导入工作台 | 已完成前端工程与生产零写入验收（真实导入仍需授权数据与人工确认） |
| 2.27 | 持牌行情导入回执与覆盖审计 | 已完成工程与生产空历史验收（真实回执待首次授权导入后形成） |
| 2.28 | 结果标签生命周期与执行可交易门禁 | 已完成工程与生产规则验收（真实停牌/涨跌停成交证据仍待供应商接入） |
| 2.29 | 停牌/涨跌停可交易事实与导入门禁 | 已完成工程与生产空数据验收（真实授权状态文件与人工审批待提供） |
| 2.30 | 可交易状态人工导入工作台 | 已完成前端工程与生产零写入验收（真实导入仍需授权数据与人工确认） |
| 2.31 | 无新闻研究人工录入与三段预校验 | 已完成前端工程与生产零写入验收（真实分析师证据及计划批准待人工输入） |
| 2.32 | 第二期生产就绪度与行动队列 | 已完成工程与生产真实缺口验收（最终留出评估仍为显式工程缺口） |

### 当前真实验收缺口

截至 2026-09-10 北京时间，生产库的分析师证据、已批准定时研究计划、基准映射、成熟前瞻标签、滚动数据集、实验和效果报告均为 0；`SEC_IDENTITY` 仍未配置。以上项目依赖真实人工批准、可识别的 SEC 访问身份或未来 1/5/20 个交易日自然经过，不能通过演示数据、自动批准或提前标签补齐。当前 66 条前瞻预测全部保持 pending，所有活跃市场的已批准基准覆盖仍为 0；恒指持牌总回报与可交易状态导入门禁已经就绪，但尚未取得真实授权文件、许可证引用和人工审批。因此第二期尚未达到第 5 节的完成定义。

## 5. 第二期完成定义

第二期只有在以下条件全部满足后才能标记完成：

- M1 至 M5 的代码、调用链、数据库迁移、自动化测试和生产读回全部通过。
- 已使用真实生产来源形成可审计的 PIT 数据集和成熟标签。
- 至少一个股票市场完成滚动训练、独立校准和从未参与选择的未来留出测试。
- 效果报告同时披露覆盖率、失败样本和统计不确定性，而非只报告成功预测。
- 模型切换和回滚演练完成，且没有未经人工批准的自动上线路径。
- M6 仍为可选增强；未证明增量价值的功能可以保持关闭或不适用，但必须明确记录原因。

## 6. 交付记录

### 2.1 第二期需求冻结与不可变行情观测

- 新增 `market_price_observations`，不可变保存资产、市场、币种、交易日、观测时间、首次可获得时间、价格字段、时间精度和来源身份。
- `close` 与 `adjusted_close` 使用不同身份，普通收盘价不能冒充复权价。
- 现有真实结果任务从 FMP、Market Adapter 或 CoinGecko 取得资产及基准价格后，将标准化结果幂等写入真值表。
- 重复抓取保留第一次 `available_at`，避免后来补采的数据伪装成历史时点已经可用。
- `GET /go/market-prices/{assetID}` 分开接受价格观测截止和系统可获得截止；查询会选择当时已经可用的最新供应商修订。
- 本步骤只建立行情真值基础，不宣称公司行动、历史证券池或自动估值已经完成。

### 2.2a 公司行动真值层

- 新增 `corporate_action_observations`，覆盖现金分红、拆股、合股、代码变化、停牌和退市，并分别保存生效、观测、首次可获得、公告、登记、支付和结束时间。
- 供应商同一事件的后续修订保留为新记录；时点查询只返回截止时已经可获得的最新修订，后补历史数据不会泄漏到过去。
- FMP `/dividends` 与 `/splits` 已接入主数据 Worker；手工同步使用 `POST /go/corporate-actions/{assetID}/sync`，定时基本面刷新也同步对应公司行动。
- `GET /go/corporate-actions/{assetID}` 支持分别指定生效截止、系统可获得截止和事件类型。
- 普通拆股与合股由比例方向严格区分，现金分红同时保留原始和供应商复权金额；来源 URL 会去除凭据。
- 本子步骤仅交付公司行动；基准映射和历史证券池由后续 2.2b 独立交付。

### 2.2b 时点基准映射、历史证券池与质量披露

- 新增 `benchmark_mapping_observations`，按资产、行业、市场或策略保存不可变基准政策；每条记录包含精确基准资产 ID、双方市场与币种、有效区间、首次可获得时间、来源、理由和审批人。
- 管理员通过 `POST /go/benchmark-mappings` 显式批准映射，请求必须携带幂等键；跨币种映射、基准资产不存在、资产自我基准和身份不一致会被拒绝。迁移不自动填入任何猜测或倒填的历史策略。
- `GET /go/benchmark-mappings/{assetID}` 按有效时点和可获得截止解析优先级；历史行业只能由调用方提供当时的行业上下文，禁止读取今天的行业分类替代过去。
- 结果计算已移除按市场写死的 SPY、沪深 300、恒指和 BTC 路径。信号产生时尚未批准映射、精确身份/币种不完整或基准价格缺失时，`benchmark_return` 与 `alpha` 保持空值，并记录结构化 `unavailable` 原因。
- 旧版硬编码路径已经生成的相对收益不会继续冒充已批准结果：迁移将原值保存在 `legacy_benchmark_audit`，公开字段置空并标记 `legacy_unapproved_mapping`；绝对收益仍保留。
- 新增 `security_universe_snapshots` 与 `security_universe_memberships`。每次主数据刷新不可变保存供应商、纳入条件、纳入、排除、供应商明确退市和失败状态；供应商列表缺失只标记为排除，不推断为退市，失败刷新不清空现有证券。
- `GET /go/security-universes/{universeID}/snapshots` 和 `GET /go/security-universe-memberships/{assetID}` 支持按可获得时点读回历史；`GET /go/market-data-quality` 汇总基准缺失、证券池失败/排除/退市、行情和公司行动覆盖，并公开各类缺失处理规则。
- PostgreSQL 回归测试覆盖事后批准不回灌、幂等审批、币种冲突、失败快照、未来快照隔离，以及纳入/排除/退市三类成员记录。

### 2.3 无新闻定时基本面研究

- 管理员通过 `PUT /go/fundamental-research/{assetID}/schedule` 显式批准不可变计划版本；请求必须携带幂等键，并绑定已经存在的可用预测版本、证据化估值情景和评级政策。
- 调度器每小时检查到期计划，单次最多处理 10 个标的；每个计划按自己的 cadence 决定实际运行时间。
- 到期运行先刷新 FMP 财务快照和真实价格观测，再以最新可用复权价重算估值与评级；不自动创造或修改财务假设。
- 若预测版本之后出现新财报、计划过期、复权价缺失或过期，则返回明确状态；新财报和计划过期会将计划转为 `review_required`。
- 相同预测、计划和价格时点产生相同估值/评级身份，重试不追加重复评级修订；数据刷新或技术失败最多一小时后重试。
- `GET /go/fundamental-research/{assetID}/schedule` 查看完整计划历史，`DELETE` 由管理员暂停当前计划；前端“基本面与预测”页提供批准、状态查看和暂停入口。
- PostgreSQL 端到端回归以同一财务快照、预测输入、估值情景、价格证据、基准证据、评级政策和有效时点分别执行定时与手动入口，并断言复用同一个预测版本、估值版本和评级修订；入口来源标签不进入计算身份，也不会制造重复评级历史。

### 2.4 多期限真实结果标签与质量报告

- 冻结 `prediction-outcome-label-v1`，只接受 `absolute_up` / `excess_up` 与 1、5、20 个交易日；中性区间、入退出、复权价格和 alpha 算术定义均在观察未来结果前固定。
- outcomes Worker 已接通 `prediction_runs → outcome_records`：各期限独立成熟，未成熟记录保持 pending；未预注册 v1 的旧预测、预测无分数、口径不支持和资产类别未实现分别保存为 excluded。
- 股票只接受供应商明确标记的 `adjusted_close`。信号后第一个可观测交易日入场，入场后第 N 个可观测交易日退出；周末、节假日、停牌和历史补采不会按自然日提前成熟。
- 相对目标只使用信号时已经批准的精确基准映射，且要求同币种、相同入退出交易日期；缺基准或基准缺日时保留绝对研究结果，但相对目标标记 unavailable。
- 分开保存 `raw_return`、`benchmark_return`、`excess_return`、风险调整残差状态和模拟净收益。没有模型注册时预存的执行成本/滑点/资金费用时，`net_return` 为空且明确标记 `research_result_only`。
- `GET /go/outcome-labels/{assetID}` 读回完整标签历史；市场数据质量接口增加 mature、pending、unavailable、excluded 标签计数。
- 口径详见 `docs/outcome-label-definition-v1.md`；单元回归覆盖绝对上涨但相对落后、1/5/20 独立成熟、盘后/周末和迟到补采，PostgreSQL 端到端回归覆盖 Worker 幂等写入。

### 2.5 滚动前推数据集与泄漏门禁

- 新增 `walk-forward-dataset-v1`：训练窗口按固定长度真实滚动，校准和未来测试使用独立时间窗口，分区之间设置显式 embargo。
- 每个样本都校验特征截止、预测创建时间、标签入退出窗口和标签可获得时间；标签窗口跨越分区边界或在该轮截止时尚未成熟会被排除，embargo 不替代逐样本 PIT 检查。
- 事件 ID 与转载簇按传递闭包隔离；同一事实不能跨训练、校准、测试或最终留出集，所有排除项保存结构化原因。
- 新增管理员限定的最终留出集预注册接口。预留必须发生在首个信号前，目标、期限、区间、标签截止和批准人冻结；密封样本不通过 API 暴露给日常调参或自动模型选择。
- 新增数据集版本、折次、样本清单和密封留出样本四类持久化表；内容摘要、幂等键、折次边界、完整开发样本时点和排除原因可读回复现。
- 没有合格最终留出样本时拒绝构建。生产环境若尚无模型、预测和成熟标签，只代表能力已部署，不代表真实数据验收或最终留出评测已完成。
- 完整契约与接口见 `docs/walk-forward-dataset-contract-v1.md`；单元回归覆盖滚动窗口、逆序输入复现、未来特征、事后预测、标签跨界、传递转载簇和密封留出，PostgreSQL 回归覆盖迁移、幂等持久化与 API 清单脱敏。

### 2.6 可解释基线、消融和独立校准

- 新增 `baseline-calibration-experiment-v1`，按每个滚动折次分别读取训练、独立校准和未来测试分区；SQL 固定排除 `sealed=true` 及负折次，最终留出集不能进入开发实验。
- 同口径比较历史正类率、简单规则、LLM 方向分、可解释逻辑回归以及价格、基本面、新闻、预期修订四组消融。LLM 分数或完整特征缺失时明确标记不可用，不把缺失填零。
- 所有逻辑回归和消融在完整全特征的共同样本上运行；均值、尺度、系数与模型版本只由当轮训练样本产生，未来测试结果不会反向进入预处理或拟合。
- 二分类模型使用独立 calibration 分区拟合 Platt 校准器，至少要求 30 个独立事件簇和正负两类；资产类别、市场、期限、模型版本、时间范围或有效期不匹配时保持 `uncalibrated`。
- 未来 test 分区报告 accuracy、正类率、预测偏斜、Rank IC、Brier、log loss、ECE、可靠性分箱及每箱 Wilson 95% 区间；逐样本分数、概率、标签和收益可追溯。
- 实验制品、模型预处理、校准器、消融差异和机器可读报告均不可变持久化，幂等键不能绑定不同实验；实验不会自动注册、批准或替换生产模型。
- 完整口径见 `docs/baseline-calibration-experiment-v1.md`。生产尚无模型、数据集和成熟标签时，只能确认能力已部署，不能声称真实基线优于简单规则或概率已经通过市场验收。

### 2.7 分层效果报告、Shadow 和故障演练

- 新增 `layered-performance-report-v1`，将研究质量、信号预测与可执行模拟严格分层；事实、关系、引用支持和拒答合理性使用不可变人工逐维标签计算准确率与区间，缺标签保持不可用；覆盖率和准确率同时披露，看空/非看多与看多方向分别报告绝对和超额收益。
- 信号结果按独立事件簇和时间折次报告，不汇总伪独立显著性；accuracy 带 Wilson 95% 区间，小样本明确警告，概率校准与方向/收益指标保持不同字段。
- 数据质量公开候选、排除/拒绝、未成熟、缺失、技术失败和全部排除原因；报告可定位数据集、实验、逐样本结果及制品版本，但不暴露密封最终留出样本 ID。
- 没有显式成本时净收益保持空值；没有组合权重、仓位和权益曲线时换手、敞口和策略回撤保持不可用，禁止使用资产回撤替代。
- 模型注册时在不可变范围中预注册独立样本、Shadow 周期、最大 ECE 和回滚触发条件；晋升请求不能事后降低门槛。候选晋升后同资产类别/目标/市场/期限的旧正式模型转为 `superseded`；新增显式人工回滚接口，只允许恢复同范围、曾经获批的版本，全部切换写入持久治理审计。
- 新增五类非生产变更故障演练：数据源不可用、模型超时、特征分布漂移、校准过期和人工回滚门禁。数据库约束禁止演练修改生产状态；漂移、超时和质量下降只能告警、降级或要求人工回滚。
- 完整契约与接口见 `docs/layered-performance-and-governance-v1.md`。开发报告提升仍不能自动上线；真实前瞻样本、成熟结果、生产告警与持续演练必须另行积累。

### 2.8 反方研究与新增资产模型验收

- 反方研究继续默认关闭。反证必须精确关联核心 claim，使用截止时可得、首轮未引用且底层来源独立的证据；候选错误不改写主报告或置信度。
- 消融入口要求唯一事件簇、相同输入截止、固定新旧版本、时间折次和独立真值来源；报告错误发现率、误挑战率及 Wilson 95% 区间，同时披露额外延迟、成本和逐折稳定性。
- 少于 100 个独立复核、20 个 baseline 错误、三个稳定时间折次，或统计区间未过门槛时保持 `keep_disabled`；达标也只进入人工复核，不能自动开启。
- `market-policy-v3` 分别冻结股票、ETF、加密资产和商品的币种、时区、日历、时点基准政策、专用研究输入及执行约束；不适用的股票式财务方法保持 `not_applicable`。
- 新增数据库驱动的分市场就绪报告，按 `asset_class:market` 披露资产、模型、预测、同范围校准和成熟结果；先要求至少一个股票市场完成端到端前瞻验收，再允许 ETF、加密或商品进入人工验收判断。
- 完整契约见 `docs/optional-counter-research-and-market-readiness-v1.md`。生产没有合格模型和成熟样本时，新增资产效果仍是不可用，不以开发完成度冒充增量价值。

### 2.9 真实生产数据冷启动

- 每日事实数据刷新除显式预测/评级标的外，允许纳入最近 30 天由当前 `llm-direction-v3` 证据契约完成研究的美股标的，打破“先有预测/评级才同步财务”的循环依赖。
- 冷启动仅同步 FMP 财务快照和公司行动；旧评分版本、过期研究、非美股和非股票资产不能借此进入当前基本面流程。
- 结果记录 `recent-research-fundamental-bootstrap-v1` 选择策略及逐标的选择原因，并明确 `automatic_assumptions=false`、`automatic_ratings=false`、`automatic_predictions=false`。
- 这一步不把旧推荐评分当作收益标签，不注册伪造样本的模型，也不发布未校准概率。显式财务假设、估值情景、基准和评级政策仍需由分析师批准；真实预测和成熟结果仍按时间自然积累。
- PostgreSQL 隔离回归覆盖显式跟踪优先、当前契约近期研究准入，以及旧契约、过期研究和跨市场标的排除。

### 2.10 前瞻规则基线样本采集

- 新的 `llm-direction-v3` 推荐与一项 outcomes 采集任务在同一事务中写入，避免研究成功但样本采集任务丢失；任务只处理自身绑定的新推荐，不扫描或倒填已经看到结果的历史推荐。
- 单并发研究 Worker 使用完成优先队列：已由事件阶段产生的标的研究先于新的上游事件研究执行，其中可进入本阶段前瞻采集的 NASDAQ、NYSE、AMEX 标的优先；其他市场的标的研究随后继续执行，不删除或跳过。该顺序避免事件积压及跨市场积压使推荐、预测与结果链路长期饥饿；双并发时仍保留 fast/preferred 分工。
- 为美股分别冻结 1、5、20 个交易日的 `llm_direction_score` 恒等规则基线。它保留原始大模型方向分作为 `raw_score`，`sample_count=0`，不声称经过训练，也不输出概率。
- 规则基线永久标记为 `forward_data_collection_only`、`promotion_eligible=false`；模型晋升接口返回 `fixed_rule_data_collection_only`，校准注册接口也拒绝把该采集基线包装成已校准概率。
- 预测记录保留推荐 ID 作为特征来源，并沿用原始 `signal_available_at`。重复任务使用同一预测身份；早于该基线首次有效时间的信号被跳过，不能事后回填。
- PostgreSQL 端到端回归验证三期限建模、预测写入、无概率、任务幂等、历史信号拒绝和晋升阻断。真实成熟标签仍需等待未来 1、5、20 个交易日，并继续遵循行情、公司行动和基准质量门禁。

### 2.11 无新闻基本面人工准备包

- 新增 `GET /go/fundamental-research/{assetID}/preparation`，只从请求截止时已经可用、报告期和期间类型完全一致的利润表、资产负债表和现金流量表构建只读准备包；禁止混用不同报告期凑齐输入。
- 准备包从原始财务事实推导收入、营业利润率、实际税率、折旧摊销、资本开支、营运资金变化和稀释股数，并对每个字段保存快照 ID、原始指标和值及计算变换。资本开支统一为正的现金流出，供应商现金流表中的营运资金符号显式反转。
- 缺少任一事实时对应字段保持 `null` 并返回 `missing_fields`，不以零填充；截止时间之后的财报修订不会进入准备包。
- 页面可将准备包载入无新闻研究 JSON 编辑器，但估值情景、资本成本或可比公司证据、基准预期、原因码、失效规则和批准人仍保持人工输入。响应明确声明不自动生成假设、估值或评级。
- PostgreSQL 隔离回归覆盖同报告期选择、未来修订隔离、比例推导、现金流符号标准化、字段级来源，以及未产生任何受治理输入或批准状态。

### 2.12 一致预期首次观测与前瞻修订历史

- 新增 FMP `analyst-estimates` 年度一致预期适配器，采集收入、EPS、EBITDA、EBIT、净利润和销售管理费用的 low、mean、high，以及供应商提供的收入/EPS 分析师样本数；期间、币种、统计方式、口径和来源记录分开保存。
- FMP 当前接口不提供预测的发布或修订时间，因此系统把本地抓取时点明确记录为 `first_observed_at`，响应和原始载荷均声明 `provider_publication_time_available=false`、`historical_backfill=false`。不得将首次抓到的当前值伪装成公告前历史预期。
- 新增管理员接口 `POST /go/consensus/{assetID}/sync` 和只读接口 `GET /go/consensus/{assetID}`；只读查询严格应用 `as_of` 截止，首次观测前返回空列表。
- 每日受跟踪/近期完成研究的美股事实刷新同时采集一致预期，从上线日起前瞻积累真实修订序列。管理层指引继续与一致预期分表，并只接受带来源、适用期间和真实发布时间的管理员导入。
- 同一期间的供应商原始汇总记录未变化时不追加重复快照；任一字段变化时按新的首次观测时点保存整条汇总记录对应的指标范围，以便复原每次真实修订的覆盖范围。
- 一致预期采集只写事实快照，不生成预测假设、估值或基本面评级。PostgreSQL 隔离回归验证首次观测时间、未来数据隔离、完整来源、无历史倒填和无评级副作用。

### 2.13 公告语义与管理层指引修订

- 新增 `announcement-expectation-semantics-v1`，将同比变化与公告前一致预期偏差分开计算和返回；能够分别表达 `growth_below_consensus` 与 `decline_above_consensus`，不再把业务增长和市场超预期混为一个方向。
- 同比要求资产、指标、期间类型、币种、单位和会计口径一致，并要求可比较的上年同期；上期值小于或等于零时只保留绝对变化，简单百分比明确为 `unavailable_non_positive_prior`。
- `POST /go/consensus/{assetID}/announcement-assessment` 只读取严格早于公告时点的一致预期；期间、币种、单位或 GAAP/非 GAAP 口径不一致时保持 unavailable，不降级为猜测。
- 一致预期只从供应商汇总快照计算修订方向、绝对幅度和样本数变化，并明确 `individual_analyst_behavior_status=unavailable_aggregate_snapshots_only`，不得推断单个分析师行为。
- `GET /go/consensus/{assetID}/guidance` 按可用时点读回管理层指引旧值、新值、区间、适用期间和来源，并计算上调/下调、区间收窄/扩大及变动边界；响应明确 `guidance_is_consensus=false`。
- PostgreSQL 隔离回归覆盖指引修订前截止、旧/新区间持久化和修订方向；单元回归覆盖“同比增长但低于预期”“同比下降但好于预期”、非正上期值以及聚合预期修订边界。

### 2.14 SEC 原始披露候选与管理层指引人工确认

- 新增 SEC EDGAR 官方披露适配器。它先从官方公司代码目录解析 CIK，再读取公司 submissions JSON，只把 `8-K`、`10-Q`、`10-K`、`6-K`、`20-F`、`40-F` 及其修订表单保存为“可能含指引的待核验文档”；遵循 SEC 自动访问要求并强制配置可识别的 `SEC_IDENTITY`，无身份时明确不可用。
- `guidance_source_documents` 不可变保存 CIK、accession number、表单、报告日、申报日、SEC 接收时间、首次本地观测时间、申报索引页、主文档和原始元数据。SEC 明确说明接收时间到网页可访问通常仍有 1—3 分钟且不保证，因此 `accepted_at` 保留官方时间，`source_available_at` 保守使用首次本地观测时间；历史截止早于首次观测时文档不可见，不做事后倒填。
- 文档候选明确标记 `candidate_is_guidance=false`、`automatic_extraction=false`。发现财报或 8-K 不会自动生成指引、假设、评级或概率，也不会把申报表中的任意前瞻叙述猜成数值区间。
- `GET /go/consensus/{assetID}/guidance-sources` 按时点返回候选和最新人工核验状态；`POST .../guidance-sources/sync` 仅排队官方文档同步；`POST .../guidance-sources/{documentID}/reviews` 保存幂等的 `confirmed_guidance`、`no_guidance` 或 `needs_follow_up` 审核。
- 只有 `confirmed_guidance` 且人工提供适用期间、指标、币种、单位、会计口径、原文摘录、披露位置、同一 accession 目录中的证据 URL 和审核人时，才在同一数据库事务内生成管理层指引快照。`published_at` 和来源身份由 SEC 候选强制覆盖客户端值，`available_at` 使用人工核验完成时间；事后人工提取不会回灌到核验前的历史回放。
- 每日受跟踪美股事实刷新在 SEC 身份已配置时同步披露候选；身份缺失只报告 `sec_identity_not_configured`，不影响已有财务、一致预期和公司行动刷新。生产当前尚未配置 SEC 身份，因此真实候选数和人工确认数仍必须保持为零/不可用，不能据此声称 P1-02 已通过真实指引历史验收。
- 单元回归覆盖 User-Agent、CIK 解析、平行数组完整性、支持表单、官方时间语义和跨 accession 证据 URL 拒绝；PostgreSQL 回归覆盖候选幂等、公告前截止、人工确认、来源字段强制覆盖、核验幂等冲突以及无评级副作用。

官方数据契约参考：[SEC EDGAR 数据 API](https://www.sec.gov/search-filings/edgar-application-programming-interfaces) 与 [SEC EDGAR 访问说明](https://www.sec.gov/search-filings/edgar-search-assistance)。

### 2.15 无新闻研究真实复权价格冷启动

- 新增管理员限定的 `POST /go/market-prices/{assetID}/sync`，把单标的最近 1—90 日真实供应商行情排入主数据 Worker；默认同步最近 14 日。美股明确使用 FMP `historical-price-eod/dividend-adjusted`，因为 `historical-price-eod/full` 只提供普通 OHLC，不能满足复权标签契约。该入口与定时研究和真实结果标签复用同一价格适配器、标准化逻辑与 `market_price_observations` 不可变存储，不建立第二套价格口径。
- 同步结果分别报告供应商返回数、复权收盘价数、新增数和累计数；没有可靠 `adjusted_close` 时返回明确 `unavailable`，普通收盘价不能冒充复权价格证据。
- 供应商在交易时段内可能提前返回仍会变化的当日 EOD 行。日期型行情必须经过市场本地收盘时间门禁，并额外保留 15 分钟发布缓冲；美股、A 股和港股分别按各自本地时区判断，收盘前的当日行不写入 `daily_close`。
- 同步只写行情事实，响应和 Worker 结果明确 `automatic_assumptions=false`、`automatic_valuation=false`、`automatic_rating=false`；不会因为取得价格而生成预测、估值、评级或概率。
- “基本面与预测”页面可显式同步并读取最近复权价格，展示价格观测 ID，供人工填写的研究输入引用。财务事实模板仍不自动把价格写入审批内容，分析师必须确认价格时点与证据适用性。
- PostgreSQL 隔离回归覆盖真实适配器形状、复权价优先、来源 URL 去凭据、首次可获得时间、重复同步幂等，以及没有预测/评级副作用；生产验收需要对样板标的同步并读回非零不可变价格观测。
- 生产验收（2026-09-10 北京时间）：NVDA 同步任务 `1d1bea0b-c28d-4637-a54f-efacc0977c2d` 完成，接纳 9 条历史 `adjusted_close`、新增 0 条、总库存 19 条；此前误收的 2026-09-09 盘中日线按唯一证据 ID 清理后，再同步仍以 2026-09-08 为最新复权收盘。数据库中 NVDA 的预测版本、估值、评级修订/状态和研究计划均为 0，验证该入口只补价格证据、不自动生成结论。

### 2.16 人工研究到定时计划的同源交接

- 人工无新闻研究成功后，API 同响应返回可直接用于计划审批的 `schedule_draft`，绑定本次实际持久化的 `forecast_version_id`，并原样复用人工提交的估值情景、基准预期证据、原因码、假设变化、研究证据和失效规则，避免跨输入框手工复制造成版本或证据错配。
- 草稿主动移除人工运行使用的 `as_of_price` 与价格证据；定时运行仍必须按执行时点重新读取不可变 `adjusted_close`，一次性价格不会被固化为未来价格。
- `approved_by` 始终为空，控制字段明确 `approval_required=true`、`automatic_approval=false`。前端只自动载入草稿，不自动调用批准接口；分析师必须复核并填写审批身份后才能创建计划。
- 输入不足、估值失败或评级不可用时不返回计划草稿。回归测试验证草稿不包含一次性价格、保留治理输入、失败工作流不生成草稿。
- 生产验收（2026-09-10 北京时间）：提交 `ad9063d` 已部署，API 健康、网页 200 且新草稿提示可读；NVDA 仍返回 `analyst_review_required`，其预测、估值、评级修订/状态和研究计划计数均保持为 0。成功工作流的草稿契约由 Go CI `34396896697` 中的隔离 PostgreSQL 回归验证，生产未在缺少真实分析师输入时制造演示结论。

### 2.17 分析师证据登记与引用硬门禁

- 新增不可变 `analyst_evidence_records`，保存资产、证据类型、标题、分析理由、类型化数值、观测/可获得/批准时间、来源名称、原始文档身份、去凭据 URL、审批人、幂等键和请求摘要。支持预测假设、估值倍数、资本成本、基准预期、评级理由和失效规则六类证据。
- 管理员通过 `POST /go/analyst-evidence` 显式登记，`GET /go/analyst-evidence/{assetID}` 按资产、时点和类型读回；重复请求幂等，不同内容复用同一幂等键被拒绝，未来可获得或其他资产的记录不可引用。
- 人工工作流必须验证复权价格 ID 与提交价格完全一致；P/E 与 WACC 必须和对应分析师证据数值一致；相对评级的基准 ID、预期收益必须和基准预期证据一致；原因码、预测假设及失效规则必须各自存在匹配的已批准证据。普通财务、预期、指引和检索证据仍可作为附加事实引用，但任意非空字符串不再被视为证据。
- 定时计划审批复用同一硬门禁，并检查预测版本中已有假设的证据。门禁在创建计划前执行，失败不暂停现有计划、不产生新计划或评级。
- “基本面与预测”页面增加证据登记与可用 ID 读回；明确显示“ID 非空不代表证据有效”，避免操作员继续填写未登记占位符。
- 自动化验收：Go CI `34400188998` 通过全量竞态测试、冻结评估以及隔离 PostgreSQL 的迁移、不可变登记、人工工作流和定时工作流回归；Frontend CI `34400188982` 通过测试与生产构建。
- 生产验收（2026-09-10 北京时间）：提交 `dc5ebc0` 已部署，数据库存在 `analyst_evidence_records` 与计划证据契约列；NVDA 读回 `analyst-evidence-v1` 空列表。缺少 `expected_return` 的基准证据登记返回 422 且全库证据计数仍为 0；NVDA 的预测、估值、评级修订/状态、计划和分析师证据均为 0。API 健康、网页 200 且静态包含新证据提示；research worker `8dd3b1fd5e67` 未停止或重建。

### 2.18 预测、估值与评级直写入口统一证据门禁

- 管理员直写预测版本、估值和基本面评级的三个入口复用 2.17 的同一证据契约，不再允许绕过一体化人工工作流直接提交未登记占位符。
- 预测假设必须匹配字段和值均一致的 `forecast_assumption`；P/E 与 DCF 分别匹配 `valuation_multiple` 和 `cost_of_capital`；评级必须匹配真实复权价格、相对基准预期、全部原因码和失效规则。
- 评级入口在门禁阶段采用与底层评级存储一致的默认政策。调用方省略政策时仍按默认美股相对评级校验基准，不能利用默认化顺序跳过基准证据。
- 三个门禁均在底层写入前执行；只读预测、估值和评级接口不变。隔离 PostgreSQL 回归覆盖有效证据、任意字符串拒绝、WACC、预测假设、失效规则及默认相对政策的基准校验。
- 自动化与生产验收（2026-09-10 北京时间）：Go CI `34401645808` 通过全量竞态、冻结评估和全部隔离 PostgreSQL/Redis 回归。提交 `3d1f50e` 部署后，预测入口以未登记假设、估值入口以未登记可比证据、评级入口以真实 NVDA 复权价格加未登记基准分别请求，三者均返回 422；NVDA 的预测、估值和评级修订计数从 `0,0,0` 保持为 `0,0,0`。API 健康，research worker `8dd3b1fd5e67` 未停止或重建。

### 2.19 前瞻结果标签冷启动触发与幂等调度

- 新增管理员限定的 `POST /go/outcome-labels/evaluate`，仅排队每日调度已经使用的 `market_loop.evaluate_outcomes`，不建立第二套结果计算入口。
- 手动触发与每日调度共用 `outcomes` 队列、冻结的 `prediction-outcome-label-v1` 和同一活动任务去重键；已有 queued、retrying 或 running 任务时返回同一个任务 ID，避免并行评价。
- 响应明确 `early_maturity_allowed=false`。触发不会修改 1/5/20 交易日口径，不会让未到期样本提前成熟，也不会把缺失行情、基准或公司行动填成零值。
- 管理员鉴权回归覆盖未授权拒绝；隔离 PostgreSQL/Redis 命令回归覆盖 Go outcomes 队列、任务类型、冻结标签版本和重复触发幂等。
- 自动化与生产验收（2026-09-10 北京时间）：提交 `cf36741` 上线冷启动入口，任务 `4d690019-20b8-45c2-9f26-e0586fa91081` 一次完成，30 条前瞻预测全部保持 pending，成熟、排除、不可用和失败均为 0；任务不会为未成熟预测写入最终 `outcome_records`。Go CI `34402924063` 通过全量竞态、冻结评估和隔离 PostgreSQL/Redis 命令回归。
- 首次生产触发同时暴露既有历史推荐结果保存的 UUID/varchar 参数类型冲突。提交 `afa97f0` 增加显式字符串类型边界和 PostgreSQL 幂等回归，Go CI `34403997775` 全部通过；仅替换 outcomes worker 后再次执行任务 `caf5680e-ba15-4230-9930-05670ba66a6e`，一次完成并成功写入 323 条历史结果。剩余 145 条历史推荐因行情供应商返回 HTTP 502 而保留为显式失败，不伪造价格或结果；新标签子任务对 33 条预测给出 `selected=33,pending=33,matured=0,excluded=0,unavailable=0,failed=0`，数据库保持 `outcome_records=0`，证明未成熟门禁有效。
- 生产 API 健康、网页返回 200；research worker `8dd3b1fd5e67` 未停止或重建。历史行情供应商 502 属于外部数据可用性问题，继续由后续运行重试，不影响 2.19 的前瞻标签契约验收。

### 2.20 A 股复权行情端点与来源语义修复

- 2.19 的生产任务暴露出 145 条 A 股历史推荐无法取价。生产服务器对腾讯旧 `fqkline` 路径的直接请求稳定返回 WAF 501，Market Adapter 因而对调用方返回 502；同域 `newfqkline` 路径对沪深样板代码均返回 200 且继续提供 `qfqday`。
- Market Adapter 的默认 A 股端点切换到 `newfqkline`。`qfqday` 不再输出为含义模糊的 `close`，而是显式输出 `adjusted_close`；只有供应商退回未复权 `day` 时才输出普通 `close`。每条价格同时传递 `Tencent Finance`、去凭据来源 URL 和证券序列身份，结果与手工价格同步均将这些字段写入不可变行情观测。
- 回归覆盖默认端点防回退、前复权字段语义、来源 URL 去凭据、来源链路跨适配器传递，以及 PostgreSQL 中 A 股 `adjusted_close` 的幂等持久化。Go CI `34405904390` 通过全量竞态、冻结评估和全部隔离 PostgreSQL/Redis 回归。
- 生产验收（2026-09-10 北京时间）：提交 `0ebd135` 仅部署 Market Adapter、outcomes worker 和共享价格持久化所需的 masterdata worker，research worker `8dd3b1fd5e67` 未停止或重建。样板同步任务 `452dfb0e-04f8-4c40-968b-4da3ef358bc0` 一次完成，收到并新增 11 条 `002311` 前复权价格；最新记录明确保存 `adjusted_close`、`Tencent Finance`、`https://web.ifzq.gtimg.cn/appstock/app/newfqkline/get` 和 `tencent-kline:sz002311`，旧普通收盘记录保持不可变且未被覆盖。
- 再次执行结果任务 `d2fe53a8-9a98-4da8-b6ff-42bc1ce06b1c` 后，135 条 A 股历史结果成功恢复，历史结果总数增至 458，生产库新增 1191 条腾讯 A 股前复权观测；前瞻标签子任务仍为 `selected=39,pending=39,matured=0,excluded=0,unavailable=0,failed=0`。剩余 10 条失败均为已停用且已在美股证券池标记 `excluded` 的非美交易所旧身份，FMP 当前套餐返回 402；这些失败继续显式保留，没有被删除、改成美股收益或填成零值。
- Market Adapter 健康、API 健康、网页返回 200。当前数据质量接口披露 458 条历史结果均缺少已批准的 PIT 基准映射、39 条前瞻标签未成熟；这两个真实缺口继续作为第二期完成门禁。

### 2.21 美股基准资产身份统一

- 审计发现市场策略和基本面评级的默认美股基准使用了不存在的 `equity:US:SPY`，而生产主数据中的精确证券身份是 `equity:AMEX:SPY`（市场 `US`、币种 `USD`）。旧身份会使后续人工基准映射无法同时满足资产存在性和策略一致性门禁。
- 新增共享常量 `marketpolicy.USBenchmarkAssetID`，市场策略和评级默认政策统一引用 `equity:AMEX:SPY`；测试中的分析师证据、定时研究及评级样例同步采用同一身份，仓库中不再残留 `equity:US:SPY`。
- 该修复只更正代码中的规范身份，不自动创建或追溯补写任何 `benchmark_mapping_observations`。生产基准映射仍为 0，必须由分析师明确批准来源、适用范围、有效时间、理由和审批人后才能形成相对收益证据。
- 自动化验收：Go CI `34407125324` 通过全量竞态、冻结评估和全部隔离 PostgreSQL/Redis 回归。生产提交 `2f526a3` 部署后，管理员就绪接口对美股股票返回 `benchmark_id=equity:AMEX:SPY`、`benchmark_policy=approved_point_in_time_mapping`，同时仍如实显示成熟结果与同范围校准样本不足、自动发布关闭；生产主数据可读回该精确 SPY 资产。
- 本次策略代码影响 API、主数据、演进和研究路径。API、masterdata worker 与 evolution worker 完成更新后，旧 research worker `8dd3b1fd5e67` 收到 SIGTERM 即停止领取新任务，并用约 148 秒排空在途任务后退出；原任务 `93b064ec-388c-4e28-95eb-1fd3b9c24d32` 没有被强杀，最终以 `output_limit_invalid_json` 显式失败。新版 research worker `a832f1a55f52` 随后启动并继续处理持久队列，排队和重试记录未被删除。
- 生产 API 健康返回 200，数据库与 Redis 均正常；网页返回 200。此步骤解除未来美股基准审批的身份阻断，但没有替代人工审批，也不改变第二期尚未完成的结论。

### 2.22 规范基准资产复权行情冷启动

- 审计发现管理员行情同步和 masterdata worker 只接受 `active=true` 的资产，但规范 SPY 基准为避免进入普通股票研究池而保持 `active=false`。因此即使策略身份已经修正，操作员仍无法在批准映射前验证或预热基准复权行情。
- 市场策略集中冻结 US、CN、HK 和加密资产的精确基准 ID，并提供规范基准身份判定。单标的行情同步现在只接受两类资产：活跃研究资产，或代码中明确冻结的规范基准资产；普通 inactive、退市或排除资产仍被拒绝。
- 同步结果增加 `asset_active` 和 `selection_reason`。规范基准只写不可变价格事实，不改变资产 active 状态，不创建基准映射、预测、估值、评级、计划或概率；人工审批边界保持不变。
- 自动化验收：本地 `go test ./...`、`go vet ./...` 和五项冻结评估通过；Go CI `34409062373` 通过全量竞态、不可变行情与无新闻流程 PostgreSQL 集成测试、Redis 队列测试和冻结门禁。集成测试同时覆盖 inactive 规范 SPY 可以同步、普通 inactive 股票必须拒绝、重复同步幂等。
- 生产验收（2026-09-10 北京时间）：提交 `dda9589` 只重建并替换 masterdata worker，research worker 未重启。SPY 首次任务 `ec20a592-8cb3-47e6-b9de-0715f6c9f76a` 一次完成，保存 10 条 FMP `adjusted_close`；API 读回 `equity:AMEX:SPY`、USD、`daily_close`、去凭据来源 URL 和不可变证据 ID。重复任务 `f0c38bde-b556-40cc-b099-a752c21a694f` 返回 `inserted=0`、`stored_total=10`。
- SPY 仍为 inactive，生产基准映射、该资产的预测、估值、评级修订和研究计划计数均为 0；API 与网页均返回 200。生产当前仍不存在 `index:CN:000300` 和 `index:HK:HSI` 主数据身份，所以本步骤只验收美股样板，不声称 A/H 股规范指数行情已经完成。

### 2.23 PIT 基准映射人工审批工作台

- “基本面与预测”页面接入资产的实时市场策略和 PIT 基准映射解析结果，公开区明确区分“规范基准身份存在”和“映射已经批准”；缺失映射时展示服务端原始原因，不把策略默认值伪装成已批准映射。
- 管理员可从当前资产和市场策略生成市场级映射草稿。草稿默认仅从当前时点生效，不自动追溯历史；`source_name`、`source_document_id`、`source_url`、`mapping_reason` 和 `approved_by` 保持待人工填写。提交前再次校验市场、币种和基准身份必须与实时策略完全一致。
- “批准不可变 PIT 基准映射”和“同步规范基准复权行情”是两个独立操作。前者要求管理员令牌与幂等键并创建人工治理事实，后者只为策略指定的基准排队同步价格；同步行情不会自动批准映射。
- 前端回归覆盖未批准状态、治理提示、从当前时点生效的确定性草稿和缺少规范基准时拒绝生成。提交 `508e3e9` 的 Frontend CI `34410130341` 已通过 Vitest 114 项测试和生产构建。
- 生产验收（2026-09-10 北京时间）：只重建并替换 Web 容器 `cab7fe6ed484`，research worker `a832f1a55f52` 和 masterdata worker `c9ee6e2a31be` 未停止或重建。Go API 与网页均返回 200；线上静态资源 `index-BvZNEuye.js` 可读回审批标题、草稿、不可变批准、规范基准行情同步控件和精确提交 `508e3e916bd6f52c673a691a67f68b37cafec0d3`。
- 生产 AAPL 查询仍返回 `missing_point_in_time_mapping`；全市场活跃资产的已批准基准覆盖均为 0，45 条前瞻预测保持 pending、成熟标签为 0。该结果证明工作台没有自动审批、倒填映射或提前制造标签；真实来源、理由和批准人仍是第二期的人工输入门禁。

### 2.24 中港规范总回报基准身份与中证官方行情

- 官方身份复核发现旧策略使用的是价格指数短码 `000300` 和 `HSI`，不能证明与总回报定义一致。中证官方沪深300资料把全收益指数明确列为 `H00300`（路孚特 `.CSIH00300`、彭博 `CSIR0300`）；恒生指数公司官方方法资料把恒指毛总回报指数列为 `.HSIDV`（彭博 `HSIRH`、Wind `HSIRH.HI`），币种为 HKD。参考：[中证沪深300官方资料](https://oss-ch.csindex.com.cn/static/html/csindex/public/uploads/indices/detail/files/zh_CN/000300factsheet.pdf)、[恒生指数官方方法资料](https://www.hsi.com.hk/static/uploads/contents/en/dl_centre/methodologies/IM_hsie.pdf)。
- 市场策略升级为 `market-policy-v4`：A 股规范基准冻结为 `index:CSI:H00300`，港股冻结为 `index:HSI:HSIDV`；旧 `index:CN:000300` 和 `index:HK:HSI` 不再被识别为规范总回报基准。迁移 `00028` 以 inactive 的 `gross_total_return_index` 身份建立两项主数据，保留官方代码别名、市场和币种，不让基准进入普通研究证券池。
- Market Adapter 为 `H00300` 接入中证指数有限公司官方 `index-perf` 日线。请求携带明确客户端身份，逐行核对返回 `indexCode`；收到 `000300` 或其他身份时整次拒绝。通过核验的指数点保存为 `adjusted_close`，来源 URL 去除查询参数，来源文档固定为 `csindex-total-return:H00300`，元数据明确 `return_series_kind=gross_total_return_index`。
- 恒指 `HSIDV` 的官方身份已固定，但当前可访问的腾讯港股源只返回 `HSI`/基金普通价格日线，生产 FMP 套餐对港股指数又返回 402。系统因此不把 `HSI`、盈富基金普通收盘价或零值冒充总回报；同步保持 `adjusted_close_unavailable`，等待后续接入有授权且可追溯的恒指毛总回报历史源。
- 自动化验收：本地全量 `go test ./...`、`go vet ./...`、格式检查和五项冻结评估通过；Compose CI `34412119502` 通过配置与镜像构建，Go CI `34412119768` 通过全量竞态、迁移以及 PostgreSQL/Redis 集成回归。测试覆盖官方响应形状、身份错配拒绝、规范资产迁移、inactive 基准同步、来源去凭据、总回报元数据和结果计算使用新身份。
- 生产验收（2026-09-10 北京时间）：提交 `1b040de` 部署后，CN/HK 样板策略分别返回 v4、`index:CSI:H00300`/CNY 与 `index:HSI:HSIDV`/HKD，数据库读回两个 inactive 总回报身份。中证首次任务 `2bc8247a-401b-4149-a4e1-6c3ccfa6413f` 一次完成，新增 23 条官方全收益观测；最新 2026-09-09 点位为 6857.95，读回 `daily_close`、官方来源、去参数 URL、不可变 ID 与 `gross_total_return_index` 元数据。重复任务 `b4efa2b5-5af0-4ce8-a6f5-f3e65ea421ec` 返回 `inserted=0,stored_total=23`。
- 恒指任务 `0b9c618e-c011-4f7f-b7b0-5dc2bb58ca7c` 明确返回 `adjusted_close_unavailable`、零观测和零写入。全库基准映射仍为 0，两个基准身份对应的预测、估值、评级修订和研究计划均为 0；48 条前瞻标签继续 pending、成熟标签为 0。部署时原研究任务 `0b59589c-4878-4ffe-82bc-49d2dcd2e3d6` 在旧 Worker 中完成后才启动新 Worker `12cb3f487f60`，没有强杀或丢失在途任务；API、Market Adapter 与网页均返回健康状态。

### 2.25 持牌恒指总回报行情导入门禁

- 继续核验恒生官方 [INdex360 历史数据页面](https://www.hsi.com.hk/index360/eng/indexes?id=00001.00) 后确认：页面的 `dailyClose`/`valueHistory` 数据请求依赖登录后保存在客户端的 `ACCESS_TOKEN`，未认证请求返回 401。项目不能代替数据使用方注册、接受许可条款或猜测平台内部 `HSIDV` 代码，因此不绕过认证，也不把公开 HSI 价格指数或 ETF 收盘价降级冒充毛总回报历史。
- 新增管理员限定的 `POST /go/market-prices/{assetID}/licensed-import` 和 `licensed-benchmark-price-import-v1`。入口只接受市场策略冻结的规范基准，并进一步要求主数据同时满足 `asset_class=index`、`instrument_type=gross_total_return_index`；`vendor_code` 必须精确匹配主代码或官方别名。普通指数、ETF、活跃研究证券和不存在的身份均被拒绝。
- 单次可提交 1—1000 条 `YYYY-MM-DD` 交易日与正有限 `adjusted_close`。价格字段、日线精度、市场、币种和 `return_series_kind=gross_total_return_index` 均由服务器从规范身份强制确定；`available_at` 只能使用服务器实际接收时间，客户端不能把事后取得的历史文件倒填为过去已知。
- 来源名称、文档 ID、绝对 HTTPS URL、许可证引用、审批人和 `Idempotency-Key` 必填。来源 URL 在持久化前去除用户名、查询参数和片段；许可证引用和审批人只保存在管理员回执，不复制到公开行情元数据。公开观测只保留不含凭据的来源与审计回执 ID。
- 回执预约、全部行情写入和回执完成在同一 PostgreSQL 事务内执行。同一幂等键对等价但顺序不同的交易日列表返回原回执；内容变化复用同一键会整笔回滚。导入不自动建立 PIT 基准映射，不生成预测、估值、评级、研究计划或概率。
- 自动化验收：提交 `4117a09` 的 Go CI `34414976447` 通过全量竞态、五项冻结评估、迁移重放及隔离 PostgreSQL 回归；回归覆盖两日原子导入、规范身份与别名、服务器首次可得时间、URL 去凭据、敏感审批字段不进入公开元数据、顺序无关幂等、冲突回滚、错误代码拒绝和无下游副作用。
- 生产验收（2026-09-10 北京时间）：仅替换 Go API 为容器 `d358b2b1679a` 并执行迁移；research worker `12cb3f487f60`、masterdata worker `daaa4be35827`、Market Adapter `d73e94790f0a` 和 Web `cab7fe6ed484` 均未重启。API 与网页返回 200，未授权导入返回 401；新回执表存在且回执数为 0，`index:HSI:HSIDV` 行情数和全库已批准基准映射数仍为 0。该状态表示持牌数据接入能力已就绪，不表示许可证或真实恒指毛总回报历史已经取得；获得授权文件和真实审批后才能执行首次生产导入。

### 2.26 持牌总回报行情人工导入工作台

- “基本面与预测”管理员工作台新增独立持牌导入区。它只在当前市场策略的规范基准为 `index:CSI:H00300` 或 `index:HSI:HSIDV` 时生成模板，不能把 SPY、普通价格指数、ETF 或当前研究标的误当成该入口的总回报指数。
- 授权信息与行情分开录入：授权 JSON 必须包含规范 vendor code、真实来源、文档 ID、绝对 HTTPS URL、许可证引用和审批人；行情接受 `session_date,adjusted_close` 两列 CSV 或同字段 JSON 数组。客户端先校验身份、必填项、HTTPS、有效日期、正有限数、1—1000 条上限和重复交易日，并按日期排序；服务端 2.25 门禁仍是最终权威校验。
- “我确认这些观测来自有权使用的数据”复选框是显式提交门槛。授权或行情内容任一修改都会撤销确认并轮换幂等键；成功响应展示不可变回执、观测数和首次新增数，随后清空行情输入。页面持续声明导入不自动获取数据、不批准 PIT 映射、不生成评级，也不倒填可获得时间。
- 自动化验收：提交 `d1cc0f3` 的 Frontend CI `34416054239` 通过 115 项 Vitest 和生产构建。纯函数回归覆盖 HSIDV 模板、CSV/JSON 解析、日期稳定排序、数字转换、重复日、HTTP 来源、错误 vendor code、缺许可证和非适用 SPY 拒绝；静态渲染覆盖持牌入口与不自动授权提示。
- 生产验收（2026-09-10 北京时间）：只替换 Web 为容器 `5f1241fff1a5`，线上静态资源 `index-3mEzZh9w.js` 可读回持牌导入标题、授权确认、CSV 字段和不自动批准文案；API `d358b2b1679a`、research worker `12cb3f487f60`、masterdata worker `daaa4be35827` 与 Market Adapter `d73e94790f0a` 均未重启。API 和网页返回 200，持牌回执、HSIDV 行情及已批准基准映射仍全部为 0，证明页面部署没有自动提交或制造数据。

### 2.27 持牌行情导入回执与覆盖审计

- 新增管理员限定的 `GET /go/market-prices/{assetID}/licensed-imports`，默认返回最近 20 批、最多 200 批不可变回执；响应明确 `administrator_only=true`、`license_and_approval_details_public=false`。未授权调用在查询数据库前返回 401，许可证引用和审批人不会进入公共行情响应。
- 每批 `session_start`、`session_end` 由回执绑定的真实 `market_price_observations` ID 反查并聚合，不接受客户端声称的覆盖范围。历史列表返回来源、规范 vendor code、文档身份、许可证引用、审批人、观测数、首次新增数及服务器可得时间，但省略每批最多 1000 个 observation ID，避免列表响应失控；首次导入响应仍返回精确 ID。
- “基本面与预测”页面增加管理员主动读取按钮、空历史状态、最近覆盖摘要和最近 20 批明细。未读取时不把空值显示成“零批已审计”；没有回执时明确说明不能推断已经取得许可证或导入数据。
- 自动化验收：提交 `cbfeede` 的 Go CI `34416820375` 通过全量竞态、冻结评估及隔离 PostgreSQL 回归，Frontend CI `34416820359` 通过 115 项 Vitest 和生产构建。回归覆盖管理员鉴权、分页边界、实际日期覆盖、敏感审计字段可读、空规范基准列表和历史响应省略 observation IDs。
- 生产验收（2026-09-10 北京时间）：Go API 更新为 `02f0dd7d68ab`、Web 更新为 `090890c5af85`；research worker `12cb3f487f60`、masterdata worker `daaa4be35827` 与 Market Adapter `d73e94790f0a` 均未重启。管理员查询 HSIDV 返回 `licensed-benchmark-price-import-v1` 空列表，未授权查询返回 401；中证公开行情返回 200 且不包含 `license_reference` 或 `approved_by`。线上静态资源 `index-CR5cDs6R.js` 可读回回执、管理员隐私和真实覆盖范围提示；持牌回执、HSIDV 行情和已批准 PIT 映射继续为 `0/0/0`。

### 2.28 结果标签生命周期与执行可交易门禁

- 结果成熟任务现在读取截至评价时点已经可获得的公司行动和历史证券池事实。交易日不足且存在明确退市事实时，样本以 `asset_delisted_at_signal` 或 `delisting_before_horizon_exit` 保存为 `unavailable`，保留原预测、原因、已观测/所需交易日数和生命周期证据，不再永久 pending，也不删除退市失败样本。
- 历史证券池分别记录信号时点状态和评价时点状态。信号时点只能选择当时已经生效且已经可获得的 completed 快照；后来形成的当前退市分类只用于标签终止事实，不会回填成过去的研究特征或历史成分。
- 停牌继续遵守交易日期限：有效停牌期间返回 `active_suspension`，复牌后交易日不足返回 `awaiting_post_suspension_sessions`，两者都保持 pending，直到真实交易日补足或出现明确终止事实。代码变更先保留按期限计算的连续性宽限期 `max(14, 2 × horizon_sessions + 10)` 个自然日；宽限后仍缺少完整价格链才以 `symbol_change_price_continuity_unavailable` 固化，避免无限等待。
- `PricePoint` 增加独立 `tradability_status`。只有入场和出场都具有显式 `tradable` 证据时，预登记成本模型才可以生成毛策略收益、净收益和成本；`unknown`、`suspended`、`limit_up`、`limit_down` 或未知值都会保留成熟的研究收益，但返回 `simulation_status=unavailable_tradability_evidence`、净收益为空且 `research_result_only=true`。当前日线供应商不提供可靠成交/涨跌停证据，因此生产链路明确写 `unknown`，禁止从收盘价或涨跌幅反推可成交性。
- `/go/market-data-quality` 新增退市标签、代码变更不可用和缺少可交易证据的计数，并公开退市保留、停牌等待、代码连续性及涨跌停执行处理规则。每条新结果的数据质量记录公司行动类型、公司行动空缺不能推断覆盖完整及信号/评价两个证券池状态；启用执行模拟的结果还记录入场/出场可交易状态。
- 自动化验收：提交 `4b7f441` 的 Go CI `34418563626` 通过格式、`go vet`、全量竞态、五项冻结评估、迁移重放和全部隔离 PostgreSQL/Redis 回归。本次扩展的隔离 PostgreSQL 用例同时验证明确退市样本被持久保留、普通日线不能产生模拟净收益、1/5/20 日独立成熟及重复评价幂等；纯函数回归覆盖信号时点与当前证券池隔离、停牌、代码变化宽限以及未知/停牌/涨跌停执行拒绝。
- 生产验收（2026-09-10 北京时间）：服务器代码更新到 `4b7f441`，Go API 为 `14acb3c1f0b9`，旧 outcomes worker `196aa5f32f5c` 在没有在途 outcomes 任务时由 Compose 正常替换为 `5589c84690b8`；没有强杀任务。research worker `12cb3f487f60` 仍在处理原任务，masterdata worker `daaa4be35827`、Market Adapter `d73e94790f0a` 和 Web `090890c5af85` 均未替换。API 与网页返回 200，新质量接口读回 268 条公司行动观测、63 条 pending 标签、0 条成熟/不可用/排除标签，并显示三类新计数均为 0；这些零值代表当前尚无对应自然样本，不是用演示数据制造的验收结果。

### 2.29 停牌/涨跌停可交易事实与导入门禁

- 新增不可变的 `market_tradability_import_receipts` 与 `market_tradability_observations`。每条事实绑定资产、交易日、来源观测时间、服务器可得时间、`market_close` 执行点和 `tradable`、`suspended`、`limit_up`、`limit_down`、`delisted` 状态；数据库约束保证只有 `tradable` 可以同时买卖，其余受限状态一律不可买卖。可交易事实与价格、公司行动分开存储，禁止从收盘价或涨跌幅反推。
- 新增管理员限定的 `market-tradability-import-v1` 导入契约。导入必须提供 HTTPS 来源、许可证引用、人工批准人和 `Idempotency-Key`，服务器生成 `available_at`，并在单一事务中写入回执和观测；同一幂等键可安全重放，但改变载荷会被拒绝。导入不会自动运行结果评价、评级或研究任务。
- 新增公共只读 `GET /go/market-tradability/{assetID}` 和管理员回执历史接口。公共响应只暴露安全的来源事实及逐交易日决议，不泄露许可证引用和批准人；每个交易日只有所有来源的最新事实一致时才得到明确状态，来源冲突返回 `conflict` 并关闭执行模拟。
- outcomes Worker 已消费逐交易日决议。只有入场与出场都存在明确且一致的 `tradable` 证据时，预登记成本模型才生成净收益；缺失、冲突、停牌、涨跌停或退市均保留研究结果并返回执行不可用。`/go/market-data-quality` 同步公开契约版本、观测数、受限状态数、来源冲突数及保守处理规则。
- 自动化验收：提交 `11b6eb3` 的 Go CI `34420323158` 通过格式、`go vet`、全量竞态、五项冻结评估、迁移重放及全部隔离 PostgreSQL/Redis 回归。新增回归覆盖管理员鉴权、来源/许可证/批准门禁、事务原子性、幂等重放、不同来源一致与冲突决议，以及只有入场和出场均明确可交易时才解锁净收益。
- 生产验收（2026-09-10 北京时间）：服务器代码更新到 `11b6eb3`，Go API 为 `575a9e416c06`，旧 outcomes worker `5589c84690b8` 在没有在途 outcomes 任务时由 Compose 正常替换为 `c8f269638692`；没有强杀 Worker。research worker `12cb3f487f60`、masterdata worker `daaa4be35827`、Market Adapter `d73e94790f0a` 和 Web `090890c5af85` 均未替换，API 与网页返回 200。公开 NVDA 查询返回空观测和空决议，未授权管理员查询返回 401，授权管理员查询返回空回执；生产库观测/回执为 `0/0`，质量接口读回 268 条公司行动、66 条 pending 标签、0 条成熟/不可用/排除标签及 0 条可交易观测/受限状态/来源冲突。没有为验收导入演示或伪造状态；真实可交易覆盖仍需取得授权状态文件、许可证引用和人工审批。

### 2.30 可交易状态人工导入工作台

- “基本面评级与短期预测”页面接入公共可交易状态查询，并增加管理员人工导入工作台。管理员可按当前规范股票/ETF 生成授权信息模板，选择不超过 1 MiB 的 UTF-8 CSV/JSON 文件或直接粘贴内容；文件先在浏览器读取，不会因选择文件自动上传。
- 预校验与后端 `market-tradability-import-v1` 契约保持一致：每批只接受 1—1000 条、同一交易日不可重复，`session_date` 和 `source_observed_at` 不得位于未来，状态只允许 `tradable`、`suspended`、`limit_up`、`limit_down`、`delisted`。来源、文档、许可证和批准人必填，来源必须为绝对 HTTPS 地址且不得携带用户名、密码、查询参数或片段。
- 预校验通过后页面显示覆盖日期、观测总数和各状态数量；任何文件、授权信息或资产 ID 改动都会作废原预览、确认和幂等键。只有管理员再次勾选真实授权与人工审批确认后才能提交。导入成功后保留不可变回执并刷新公共决议；即使公共刷新暂时失败，也不会把已经成功落库的回执误报为导入失败。
- 页面分别展示公共逐交易日来源一致性决议、公共来源事实和管理员最近 20 批导入回执。公共区不展示许可证与批准人；管理员回执展示由实际观测聚合的覆盖日期。页面明确缺失或冲突状态不会从价格推断，也不会自动运行结果评价、生成评级或放开执行。
- 自动化验收：提交 `945dad5` 本地 Frontend 5 个测试文件、116 项测试全部通过，TypeScript 与 Vite 生产构建通过；Frontend CI `34421794272` 同样通过测试和构建。新增纯函数回归覆盖 CSV/JSON、日期排序、状态规范化、股票/ETF 范围、重复日、未来日期、未知状态、非 HTTPS、URL 凭据和中文状态标签。
- 生产验收（2026-09-10 北京时间）：服务器代码和 Web 静态构建更新到 `945dad5`，只重建 Web 为 `0f62cb649782`；Go API `575a9e416c06`、outcomes worker `c8f269638692`、research worker `12cb3f487f60`、masterdata worker `daaa4be35827` 和 Market Adapter `d73e94790f0a` 均未替换，全部运行且重启次数为 0。API 与网页返回 200，线上静态包 `index-BM6Hxs5n.js` 可读回导入编辑器、预校验入口和构建提交；AAPL 公共接口仍返回空观测、空决议，部署没有自动导入或制造数据。

### 2.31 无新闻研究人工录入与三段预校验

- “基本面评级与短期预测”页面把无新闻人工研究闭环拆分为三个必须独立确认的阶段：先登记分析师证据，再预校验并执行一次人工研究，最后从该次研究的同源输入生成并批准定时计划。任一阶段成功都不会自动替代下一阶段的人工操作。
- 分析师证据区提供预测假设、估值倍数、资本成本、基准预期、评级理由和失效规则六类引导模板。模板只生成后端支持的字段结构，数值、时间、来源和批准人保持为空；前端不会生成分析师观点、预测值、概率、评级或审批身份。
- 第一段预校验严格检查资产身份、证据类型、RFC3339 时点及先后关系、类型对应的值域和必填字段，并把来源 URL 的用户名、密码、查询参数和片段移除。只有预览通过且再次勾选不可变登记确认后才可请求后端；后端仍负责最终的时点、证据和值一致性门禁。
- 第二段预校验按 Go API 的精确 JSON 契约检查财务事实、单位、已批准且带证据的预测假设、DCF/倍数估值情景、WACC/增长率/倍数边界、评级阈值、相对基准、原因证据和失效规则。预览明确显示事实、假设、情景、原因和证据引用数量，避免把 `null` 转换为零或把任意字符串当成有效证据。
- 人工研究成功后只生成同源定时计划草稿。第三段预校验再次检查预测、估值与评级计划、1—720 小时周期、1—336 小时价格时效、1—365 天计划时效及真实 `approved_by`；页面不会自动批准计划。资产 ID 或任一输入变化都会作废既有预览、确认和幂等键，要求重新校验与确认。
- 写入成功后的提示在刷新公开数据包时继续保留；若写入已成功而刷新失败，页面会明确区分“写入成功”和“刷新失败”，避免操作员误判并重复提交不可变记录。
- 自动化验收：提交 `c9486a5` 本地 Frontend 6 个测试文件、124 项测试全部通过，TypeScript 与 Vite 生产构建通过；Frontend CI `34423519370` 同样通过测试与构建。新增回归覆盖六类空值模板、URL 凭据清理、未知字段、类型和值域、未来时点、跨资产、空估值、评级阈值、空财务值、显式假设批准及定时计划周期边界。
- 生产验收（2026-09-10 北京时间）：只重建 Web 为 `3bbef6e31264`，线上静态资源 `index-DWux9VjU.js` 可读回“生成分析师证据模板”“预校验研究输入”“预校验定时研究计划”和构建提交 `c9486a5`。Go API `575a9e416c06`、outcomes worker `c8f269638692`、research worker `12cb3f487f60`、masterdata worker `daaa4be35827` 与 Market Adapter `d73e94790f0a` 均未替换，全部运行且重启次数为 0；API 和网页返回 200。生产库分析师证据和定时研究计划仍为 `0/0`，证明本次部署没有伪造研究输入或代替分析师审批；真实输入完成前，第二期总体状态仍是未完成。

### 2.32 第二期生产就绪度与行动队列

- 新增管理员限定的 `GET /go/phase-two/readiness`，在同一个数据库快照和服务器配置视图中汇总第二期 13 个阻断门禁，包括真实分析师证据、人工批准计划、SEC 身份、PIT 基准覆盖、自然成熟结果、最终留出预注册、滚动前推数据集、开发实验、分层报告、最终留出评估、人工质量复核、五类故障演练和人工批准模型。接口只返回身份是否合规，不暴露 SEC 身份内容。
- 每个门禁分别标记 `completed`、`waiting_human_input`、`waiting_natural_maturity`、`blocked_by_dependency`、`ready_for_manual_action` 或 `engineering_gap`，同时返回当前值、要求、依赖、责任边界和下一步行动。页面 `#/readiness` 只读展示这些事实并链接到已有基本面或来源工作台，不创建数据、不触发任务，也不替代人工批准。
- 总体状态只有在全部阻断门禁真实完成后才变为 `eligible_for_human_acceptance`，且 `automatic_completion` 永远为 `false`。当前代码虽然能预注册并封存最终留出样本，但还没有经人工批准、只能执行一次且不可用于模型选择的最终留出评估入口；该项明确返回 `engineering_gap`，不得用开发集报告冒充最终验收。
- 自动化验收：提交 `f8f04e7` 本地 `go test ./...`、`go vet ./...`、Frontend 7 个测试文件共 126 项测试和生产构建全部通过；Go CI `34425117681` 通过全量竞态、冻结门禁、迁移契约及隔离 PostgreSQL 查询，Frontend CI `34425117708` 通过测试与生产构建。回归覆盖空生产事实、全部门禁满足、管理员先鉴权、真实 PostgreSQL 聚合、状态分类和静态页面不误报完成。
- 生产验收（2026-09-10 北京时间）：服务器与 API 更新到 `f8f04e7`，Go API 为 `fb75dc205a79`，Web 使用带提交标识的构建重新创建为 `f7918e51d03d`，静态资源 `index-C3ZsWSpg.js` 可读回“第二期生产就绪度”“工程缺口”和构建提交。API、网页均返回 200，未授权就绪度查询返回 401；授权读回 `overall_status=blocked`、`automatic_completion=false`、`completed_gates=1/13`。生产真实事实为：14,398 个活跃股票资产、81 条待成熟标签、5 类故障演练已通过，其余分析师证据、批准计划、PIT 基准覆盖、成熟结果、留出预注册、数据集、实验、报告、质量复核、批准模型、两类授权导入回执均为 0，SEC 身份未配置，最终留出评估未实现。outcomes worker `c8f269638692`、research worker `12cb3f487f60`、masterdata worker `daaa4be35827` 和 Market Adapter `d73e94790f0a` 均未替换且持续运行；因此第二期仍未完成，下一项工程工作是受控的一次性最终留出评估。
