# 评级系统改造交付说明（更新至 2026-09-09）

## 基线与范围

- 审查基线：`df68c0719e5912f153e183ba776433236eda9f99`。
- 本轮开始提交：`e517b79d1737ecaccca20556928b53923ebd8bfe`，分支 `golang`。
- `df68c07..e517b79` 已完成时点化财报、一致预期、预测、估值、评级政策和评级状态的第一阶段实现。
- 本轮继续实现评级修订持久化、可检查失效规则、短期预测/独立校准/滚动评估的工程基础、模型治理门禁和受控代码演进。
- 第一方运行时仍为 Go；未引入 Python 服务或镜像。

## 数据契约和实际入口

事件影响、基本面评级和短期预测是三类独立结果，不共享一个评分字段。

- 事件研究继续使用原有研究队列；`direction_score` 只表示启发式事件影响，不是收益率或概率。
- `GET/POST /go/ratings/{assetID}` 读取或创建基本面评级；评级依赖已保存的估值运行、可追溯价格、基准和政策版本。
- `POST /go/fundamental-research/{assetID}` 在同一 `as_of` 下依次保存财务预测、估值和基本面评级；不依赖新闻事件，任一阶段数据不足时停止后续阶段并返回明确原因。前端工作台提供管理员 JSON 入口。
- `POST /go/ratings/{assetID}/invalidation-check` 用显式观测值检查论点失效规则，缺失值不会转成零。
- `POST /go/prediction-models` 注册可解释模型；`POST /go/calibrations` 只用独立样本注册 shadow Platt 校准器；`POST /go/calibrations/{version}/promotion-check` 通过正确性、样本、影子期、校准质量和人工批准门禁后才激活。
- `GET/POST /go/predictions/{assetID}` 读取或产生固定期限预测。没有有效且适用的校准器时，概率为 `null`，状态为 `uncalibrated` 或 `unavailable`。
- `POST /go/model-governance/shadow-runs/{assetID}` 以相同信号截止、特征输入和执行假设并行保存正式/候选模型结果；`GET /go/model-governance/checks` 查询持久化监控与审批记录。
- `POST /go/model-governance/promotion-check` 在门禁通过后批准指定 shadow 预测模型；周期性治理任务监控来源标识变化、覆盖率、延迟、方向偏斜、配对分数 PSI 和成熟结果，漂移只告警复核，不会自动训练、回滚或切换模型。
- `COUNTER_RESEARCH_ENABLED=false` 默认关闭反方研究；显式开启后，事件研报会在定稿前增加独立反证步骤。只有精确指向原始 claim、引用未被原报告使用且在截止时间内可用的独立原始来源证据，才记录为候选错误；候选不会改写主报告或提高置信度。`GET /go/counter-research/status` 查看模式，`POST /go/counter-research/ablation` 使用独立真值标签计算错误发现率、误挑战、额外延迟和成本。
- `GET /go/market-policies/{assetID}` 返回资产主数据决定的币种、时区、交易日历、基准、研究方法及适用状态。预测模型、校准器和预测运行现在同时按 `asset_class + market` 隔离；客户端声明不能覆盖资产主数据。股票基本面工作流会拒绝 ETF、商品或加密资产，不会把 EPS/DCF 输入硬套到这些资产。`POST /go/model-evaluation/segmented-report` 强制按资产类别与市场分别生成效果报告，不返回混合汇总。
- 前端“基本面与预测”页并列读取财务快照、预测版本、估值、评级和短期预测，不把空值显示成零。

所有写接口均要求管理员认证。读取接口保留当前系统的只读访问策略。

## 任务核验状态

| 任务 | 状态 | 已接入内容 | 尚未伪装为完成的外部依赖 |
| --- | --- | --- | --- |
| P0-01—P0-10 | 代码完成 | `internal/jobs/research*`、`outcomes*`、`p0_policy*`、发布门禁和 CI 评估命令 | 部署后仍需线上读回 |
| P1-01—P1-08 | 代码完成 | PIT 财报/预期、预测与估值版本、独立评级政策/状态/修订、失效规则 | 数据源覆盖仍受 FMP 和人工导入可用性约束 |
| P1-09 | 主流程接线完成 | 同一时点的无新闻预测→估值→评级工作流、管理员前端入口；已进入预测/评级流程的美股每日限量刷新财务快照；事件研究只读信号时点前已发布结果 | 系统不会虚构价格、可比公司、资本成本或预测假设；定时自动重算估值仍需经过批准且可追溯的动态价格与假设上游 |
| P2-01—P2-02 | 工程能力完成 | 固定期限、信号后首个交易时点、复权价、基准/币种和缺失状态 | 真实行情与公司行动覆盖必须用生产数据另行验证 |
| P2-03—P2-06 | 工程基础完成 | 簇隔离前推切分、可解释基线、独立 Platt 校准、分层报告、制品版本随训练真值变化 | 没有仓库内伪造模型、概率或收益；真实 PIT 样本训练、独立校准和未来留出结果仍未产生 |
| P2-07 | 工程闭环完成 | 同截止/假设的新旧模型 shadow 记录、周期性覆盖率/延迟/方向偏斜/PSI/成熟结果监控、持久化告警与审批审计；所有切换仍需人工批准 | 真实前瞻样本积累、风险阈值审批和故障演练必须在生产数据上持续执行，不能由代码伪造为已验证 |
| P3-01 | 代码完成、默认关闭 | 反方研究已接入事件研究编排；确定性 PIT/独立来源门禁；候选不影响置信度；提供有标签消融 API | 没有独立真值标签时只显示候选错误，不宣称实际发现率；生产保持默认关闭，开启需另行批准与观察成本 |
| P3-02 | 工程闭环完成 | 资产主数据驱动的市场政策 API；股票基本面适用性门禁；模型、校准、运行按资产类别和市场隔离；前端展示研究方法；分市场效果报告函数 | ETF/商品/加密资产的专用基本面估值插件和真实分市场样本仍不存在，因此这些资产只开放专属范围的短期预测，不生成股票式基本面评级 |
| P3-03 | 代码完成 | 受保护路径、管理员批准、候选分支测试；禁止自动合并与自动部署 | 候选补丁仍需人工审核和正常发布流程 |

## 测试分层和限制

- 单元/契约测试：不依赖外部服务，覆盖确定性计算、空值、截止时间、适用范围和权限边界。
- 隔离数据库集成测试：由 `TEST_DATABASE_URL` 显式启用；没有该变量时测试会跳过，不能宣称数据库集成已通过。
- 真实数据研究评估：需要真实 PIT 数据、成熟标签和独立校准样本。代码支持生成模型、校准和分层报告，但仓库不伪造模型制品或校准概率。没有有效制品时页面必须显示概率不可用。
- 正式模型切换还需要足够独立样本、完整 shadow 周期、未污染的最终留出集和人工批准；开发集提升不能直接上线。

## 演进与部署边界

自动代码演进只生成候选补丁。候选不能修改评级、校准、评估、治理和关键证据门禁；显式管理员批准后也只在候选分支执行检查并生成 `tested_candidate`，不会自动合并或部署。健康漂移只告警并要求人工检查。

P3 仍遵循保守上线语义：反方研究开关默认关闭；ETF、商品和加密资产虽具有独立预测范围和数据需求契约，但在专用估值实现和真实验收数据齐备前，基本面状态保持 `not_applicable`，相关缺失输入不写成零。旧预测模型或校准器缺少 `asset_class` 时仅按 `equity` 兼容读取，不能静默用于其他资产类别。

本仓库按 `AGENTS.md` 的版本交付策略执行：相关检查通过并推送 `origin/golang` 后，使用受控 SSH 流程部署；Worker 必须优雅排空，部署后验证健康、Web、受影响 API、容器与队列容量。任务清单中的“不要擅自上线”是计划约束，不覆盖仓库已确认的交付策略。
